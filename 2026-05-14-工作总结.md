# 2026-05-14 下午工作总结

## 目标
调试并修复 VPN Orchestrator 脚本 (`vpn分析/vpn_orchestrator/`)，使其能正常启动 sing-box 并通过代理连通。

## 修复的 5 个 Bug

### 1. v2rayN 安装路径错误
- **文件:** `config.py:8`
- **修复:** `BASE_DIR` 从 `v2rayN-Core\v2rayN-windows-64` 改为 `v2rayn`
- 原路径不存在，导致 `sqlite3.OperationalError: unable to open database file`

### 2. 数据库列兼容性
- **文件:** `models/profile.py:100-141`
- **修复:** 添加 `_safe()` 函数，对缺失列（如 `TransportExtra`）返回默认值
- 新版本 v2rayN 数据库 schema 与代码预期列名不完全匹配

### 3. sing-box 1.13 legacy inbound 字段
- **文件:** `core/config_builder.py:46-55`
- **修复:** 从 inbound 模板中移除 `sniff` 和 `sniff_override_destination` 字段
- sing-box 1.13.11 已删除这些 legacy 字段

### 4. sing-box 1.13 encrption 字段
- **文件:** `core/config_builder.py:96-98`
- **修复:** 删除 VLESS outbound 中已废弃的 `encryption` 字段

### 5. Windows GBK 编码兼容
- **文件:** `core/logging_config.py`、`core/db_manager.py`
- **修复:** 日志控制台使用 `_SafeFormatter` 过滤 emoji，`print_all_profiles` 使用 `_safe_print`
- 节点名称含国旗 emoji（如 `🇫🇷`），GBK 无法编码导致 `UnicodeEncodeError`

## 未解决的问题

**config.json 格式不兼容** — 脚本用自己的模板完全覆盖了 v2rayN 生成的 config.json，导致生成的配置与节点不匹配。

证据：用户用 v2rayN 原版 GUI 的"自动配置代理"功能可以正常连接相同节点，但脚本生成的 config 全部超时。

原因：我们的 `config_builder.py` 采用"完全生成"策略，丢弃了 v2rayN 原始配置中的 DNS 格式、路由规则等细节。v2rayN 生成的配置是经过实际验证的。

**修复方向（明天）：** 改为"补丁"策略 — 读取 v2rayN 生成的 config.json，只替换 `outbounds` 中的 proxy 条目，保留 inbounds/DNS/route 不变。

## 涉及修改的文件
| 文件 | 改动 |
|------|------|
| `config.py` | BASE_DIR 路径修正 |
| `models/profile.py` | `_safe()` 兼容缺失列 |
| `core/config_builder.py` | 移除 `sniff`、`encryption` 字段 |
| `core/logging_config.py` | GBK 安全 Formatter |
| `core/db_manager.py` | `_safe_print` 方法 |
| `.claude/settings.local.json` | 新增 2 条权限 |
