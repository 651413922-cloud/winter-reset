# 2026-05-15 工作总结 — VPN Orchestrator 架构重构

## 当前问题清单

### ❌ 问题 1：v2rayN GUI 共存冲突

**现象**：v2rayN GUI 运行时主动监控 `binConfigs/config.json`，检测到修改后立即覆盖或清空。

**已做**：添加 `check_coexistence()` + `find_v2rayn_gui()` 检测。

**新发现的 Bug**：`find_v2rayn_gui()` 产生误报。

```python
# runtime_manager.py 当前代码（有 bug）：
for kw in ('v2rayn', 'v2rayN'):
    if kw.lower() in name or kw.lower() in exe:  # ← exe 是全路径
        return proc
```

`proc.info['exe']` 返回完整路径 `d:\useful\加速器\v2rayn\bin\xray\xray.exe`，其中包含目录名 `v2rayn`，导致 `xray.exe` 被误识别为 "v2rayN GUI"。

**实际验证**：v2rayN GUI 已关闭（`Get-Process *v2ray*` 无结果），但 PID 23260 的 `xray.exe`（遗留进程）仍触发误报。

**修复方向**：只检查进程名（`proc.info['name']`），不检查完整路径。v2rayN GUI 进程名应为 `v2rayN` 或 `v2rayN.exe`。

---

### ❌ 问题 2：遗留代理进程未清理

**现象**：运行多次测试后，残留的 `xray.exe` 进程（PID 23260）仍在运行，占用端口。

**影响**：新启动的 Xray 可能端口冲突，或被误认为 v2rayN GUI。

**修复方向**：`RuntimeManager.start()` 启动前先调用 `stop()` 清理所有遗留的 xray/sing-box 进程。

---

### ❌ 问题 3：TUN 模式依赖

**现象**：v2rayN 的 config 使用 `sockopt.dialerProxy: tun-protect-socks`，代理出站的连接走 TUN 虚拟网卡。独立启动 Xray 时没有 TUN 接口，所有出站连接超时。

**影响**：即使 config.json 格式正确，代理也无法连通。

**已做**：`patch_proxy_outbound(standalone=True)` 自动移除 `dialerProxy`，让代理直连。

**待验证**：移除 dialerProxy 后直连是否可达（SS 服务器 `*.node-for-bigairport.win` 解析到 AWS IP，可能在中国大陆网络环境下无法直连）。

**替代方案**：
- 方案 A：在 orchestrator 中实现 TUN 模式（复杂，需要创建虚拟网卡 + 路由表）
- 方案 B：使用支持直连的节点（非 TUN 依赖）
- 方案 C：保留 v2rayN GUI 的 TUN 基础设施，只替换代理出站

---

### ❌ 问题 4：核心类型不匹配（已修复）

**现象**：v2rayN 当前使用 Xray 核心（CoreType=2），生成 Xray 格式 config。但项目代码用 sing-box.exe 启动，sing-box 不认识 Xray 格式（报错：`unknown field "loglevel"`）。

**修复**：`config.py` 添加 `get_active_core_type()` 读取 `guiNConfig.json` 自动检测核心类型，`get_active_exe()` 返回对应可执行文件：
- CoreType=2 (Xray) → `xray.exe`
- CoreType=24 (sing-box) → `sing-box.exe`

---

### ✅ 问题 5：SOCKS 端口硬编码（已修复）

**现象**：`config.py` 硬编码 `SOCKS5_PORT = 10808`，但 v2rayN config 中实际端口是动态分配的（54620 / 61013 / 64659）。

**修复**：`get_socks_port_from_config()` 从 config.json 动态读取 SOCKS 入站端口，`ProxyChecker` 自动使用正确端口。

---

### ✅ 问题 6：config 全量生成（已修复）

**现象**：`config_builder.py` 用自己的模板完全覆盖 config.json，丢失 v2rayN 的 inbounds/routing/TUN 配置。

**修复**：实现 Patch Strategy — `adapters/xray_config.py` 只替换 proxy outbound 的连接细节，保留所有其他配置。

---

## 架构变更

```
vpn_orchestrator/
├── adapters/                     # NEW: 格式适配层
│   ├── __init__.py
│   ├── xray_config.py            # config 读/写/patch（格式检测 + 字段级 patch）
│   └── outbound_builder.py       # ProfileItem → Xray/singbox outbound
├── core/
│   ├── state.py                  # ENHANCED: +FAILED, +RecoveryStrategy, +HealthSnapshot
│   ├── config_builder.py         # REFACTORED: delegate to adapters
│   ├── process_manager.py        # REFACTORED: delegate to RuntimeManager
│   └── runtime_manager.py        # NEW: process lifecycle + health + recovery
├── config.py                     # ENHANCED: +核心自动检测, +动态端口
├── orchestrator.py               # REFACTORED: RuntimeManager + 新状态机 + 冲突检测
└── daemon.py                     # UPDATED: handles FAILED state
```

## 关键设计决策

1. **Patch over Generate** — 永远只 patch proxy outbound，不重生成整个 config
2. **核心自动检测** — 读取 `guiNConfig.json` 检测 v2rayN 当前使用的核心（Xray/sing-box），自动选择对应可执行文件
3. **standalone 模式** — 独立运行时自动移除 TUN 依赖的 `dialerProxy`
4. **渐进式恢复** — RESTART → SWITCH → RESET → BACKOFF → GIVE_UP
5. **v2rayN 冲突检测** — 启动时检测 v2rayN GUI 进程，有冲突时拒绝运行

## 状态机

```
DISCONNECTED → CONNECTING → CONNECTED → DEGRADED → RECOVERING → CONNECTED
                    │                         │            │
                    ↓                         ↓            ↓
                  FAILED ←────────────────────┴────────────┘
                    │
                    └──→ CONNECTING (retry)

STOPPED ← (any state, user request)
```

恢复策略升级：0-2次→RESTART, 3-5次→SWITCH_NODE, 6-8次→RESET_CONFIG, 9-14次→WAIT_RETRY, 15+→GIVE_UP→FAILED

## 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `adapters/__init__.py` | **新增** | Adapters 包 |
| `adapters/xray_config.py` | **新增** | Config patch 引擎 |
| `adapters/outbound_builder.py` | **新增** | Outbound 生成器（SS/VMess/VLESS） |
| `core/state.py` | **重写** | +FAILED, +RecoveryStrategy, +HealthSnapshot |
| `core/runtime_manager.py` | **新增** | 进程生命周期 + 健康检查 + 恢复 |
| `core/config_builder.py` | **重构** | 委托到 adapters |
| `core/process_manager.py` | **重构** | 委托到 RuntimeManager |
| `config.py` | **增强** | +核心检测, +动态端口 |
| `orchestrator.py` | **重写** | RuntimeManager + 新状态机 |
| `daemon.py` | **修改** | 处理 FAILED 状态 |

## 当前 Bug 清单（2026-05-15 21:55）

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | `find_v2rayn_gui()` 误报（匹配 exe 路径） | 🔴 曾阻塞 | ✅ 已修复 — 只检查进程名 `v2rayN` |
| 2 | 遗留 `xray.exe` 进程未清理 | 🟡 中等 | ✅ 已修复 — `start()` 前调用 `cleanup_legacy_processes()` |
| 3 | TUN `dialerProxy` 依赖 | 🟡 中等 | 已有 `standalone` 模式，待停止 v2rayN 后验证 |
| 4 | 核心类型自动检测 | 🟢 | ✅ |
| 5 | SOCKS 端口动态读取 | 🟢 | ✅ |
| 6 | Patch Strategy | 🟢 | ✅ |

## 架构原则（2026-05-15 确定）

1. **v2rayN config 是唯一真相源** — 禁止全量生成 config，只 patch outbound 连接参数
2. **永远不实现 TUN** — 完全复用 v2rayN TUN 基础设施
3. **Orchestrator 只负责调度** — 切换节点、patch、重启、健康检查、状态机、恢复
4. **稳定编排 v2rayN runtime** — 不追求"独立运行 xray/sing-box"

---

## 2026-05-15 22:30 — 双进程架构 + TUN 模式修复

### 根本原因发现

经过测试 29/41 个 SS 节点全部超时，发现根本原因：

**v2rayN 使用双进程架构，不是单进程！**

```
v2rayN 完整架构：
  sing-box.exe (configPre.json)    ← TUN虚拟网卡 + 路由 + DNS + tun-protect-socks(:58481)
  xray.exe     (config.json)       ← SOCKS入站(:58482) + 代理出站(dialerProxy→TUN)
```

- `configPre.json` (9916 bytes, sing-box native format): TUN inbounds, DNS, 完整路由规则, clash_api
- `config.json` (1601 bytes, Xray format): 简单的 SOCKS 入站 + 代理出站
- Xray 的代理出站有 `dialerProxy: tun-protect-socks`，通过 sing-box TUN 发送代理自身连接

**为什么 standalone 模式所有节点超时：**
- 移除 `dialerProxy` 后，Xray 直连 SS 服务器
- SS 服务器 `*.node-for-bigairport.win` 在中国大陆网络下无法直连（GFW 阻断）
- TUN 模式通过 sing-box 的 tun-protect-socks → direct 路由，避免了路由循环

**sing-box TUN 需要管理员权限：**
- 创建 TUN 虚拟网卡需要 admin 权限
- 测试确认：`FATAL: configure tun interface: Access is denied`

### 架构变更

```
vpn_orchestrator/
├── config.py                     # +SING_BOX_CONFIG (configPre.json)
├── core/
│   ├── runtime_manager.py        # REWRITTEN: 双进程管理
│   │   ├── start()               # 先启动 sing-box (TUN), 再启动 Xray (proxy)
│   │   ├── stop()                # 先停 Xray, 再停 sing-box
│   │   ├── restart_xray()        # 仅重启 Xray（节点切换时 TUN 不变）
│   │   ├── is_admin()            # 检查管理员权限
│   │   ├── find_singbox_process() / find_xray_process()  # 分别查找
│   │   └── cleanup_legacy_processes()  # 清理两种进程
│   ├── process_manager.py        # +restart_xray()
│   └── config_builder.py         # standalone 默认值改为 False
├── adapters/
│   └── xray_config.py            # standalone 默认值改为 False (保留 dialerProxy)
├── services/
│   └── node_switcher.py          # _apply_and_restart() 使用 restart_xray()
├── orchestrator.py               # +管理员检查, 双进程状态处理
│   ├── print_status_report()     # 重命名以消除与 status 属性的冲突
│   └── tick()                    # 智能恢复：sing-box死→全重启, Xray死→仅重启Xray
├── daemon.py                     # +print_status_report()
├── main.py                       # +print_status_report()
└── test_admin.py                 # NEW: 管理员权限测试脚本
```

### 新设计原则

1. **双进程不分离** — sing-box (TUN) 和 Xray (proxy) 必须同时运行
2. **TUN 是基础设施** — 节点切换只重启 Xray，sing-box 保持运行
3. **管理员权限必需** — sing-box 创建 TUN 需要 admin，orchestrator 启动时检查
4. **dialerProxy 必须保留** — 防止代理自身连接形成路由循环
5. **standalone 模式废弃** — 默认 standalone=False，保留完整 TUN 配置

### 当前状态

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | find_v2rayn_gui() 误报 | 🔴 | ✅ 已修复 |
| 2 | 遗留进程未清理 | 🟡 | ✅ 已修复 |
| 3 | TUN dialerProxy 依赖 | 🔴 | ✅ 已修复 — 双进程架构 |
| 4 | 核心类型自动检测 | 🟢 | ✅ |
| 5 | SOCKS 端口动态读取 | 🟢 | ✅ |
| 6 | Patch Strategy | 🟢 | ✅ |
| 7 | 双进程架构 | 🔴 | ✅ 已实现 |
| 8 | 端到端测试 | 🟡 | ✅ 已验证 — Google 204 658ms, httpbin 200 |

---

## 2026-05-15 23:00 — v1.0 发布

### 端到端测试结果

```
管理员权限测试：
  [OK] Running as admin
  [OK] sing-box started (PID: 33452, 51.2 MB)
  [OK] Xray started (PID: 30828, 1.9 MB)
  [OK] Both processes running

连通性测试：
  [OK] Google /generate_204 → HTTP 204, 658ms
  [OK] httpbin.org/ip → HTTP 200, 1616ms, 出口 IP: 13.212.79.46
```

### dialerProxy 自动恢复

由于历史 standalone 模式补丁已删除 `dialerProxy`，新增逻辑：
- 检测 config 中 `tun-protect-socks` outbound 存在
- 若 proxy outbound 缺少 `sockopt.dialerProxy`，自动补充
- 无需手动修复 v2rayN 原始配置

### v1.0 涉及的 14 个文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `adapters/__init__.py` | 新增 | Adapters 包入口 |
| `adapters/xray_config.py` | 新增 | Config patch 引擎 + dialerProxy 自动恢复 |
| `adapters/outbound_builder.py` | 新增 | Xray/singbox 格式 outbound 生成器 |
| `core/state.py` | 重写 | +FAILED, +RecoveryStrategy, +HealthSnapshot |
| `core/runtime_manager.py` | 重写 | 双进程管理 + 管理员检查 + 智能恢复 |
| `core/config_builder.py` | 重构 | 委托到 adapters，standalone 默认 False |
| `core/process_manager.py` | 重构 | +restart_xray()，委托到 RuntimeManager |
| `core/logging_config.py` | 修改 | 共享日志配置 |
| `config.py` | 增强 | +核心检测, +动态端口, +SING_BOX_CONFIG |
| `models/profile.py` | 修改 | ProfileItem 数据类 |
| `orchestrator.py` | 重写 | 双进程编排 + 状态机 + 管理员检查 |
| `services/node_switcher.py` | 重构 | Xray-only 重启, 无限节点遍历 |
| `services/proxy_checker.py` | 增强 | 动态 SOCKS 端口 |
| `daemon.py` | 修改 | +FAILED 状态, print_status_report() |
| `main.py` | 修改 | 入口不变，方法名更新 |
| `test_admin.py` | 新增 | 管理员权限测试脚本 |

### 启动方式

```bash
# 方式 1：一键启动（管理员终端）
python main.py auto

# 方式 2：bat 文件（右键 → 以管理员身份运行）
vpn_start.bat

# 方式 3：守护进程模式（30s 健康检查 + 自动故障转移）
python main.py daemon
```

### 架构全貌（v1.0 终版）

```
┌─────────────────────────────────────────────────────────┐
│                    VPN Orchestrator v1.0                 │
├─────────────────────────────────────────────────────────┤
│  main.py          CLI 入口 (menu / auto / daemon / ...)  │
│  orchestrator.py  编排层 (状态机 + 恢复 + 调度)           │
│  daemon.py        守护进程 (周期检查 + 自动故障转移)       │
├─────────────────────────────────────────────────────────┤
│  core/                    services/        adapters/     │
│  ├─ runtime_manager.py    ├─ node_switcher ├─ xray_config│
│  ├─ process_manager.py    ├─ proxy_checker ├─ outbound_  │
│  ├─ db_manager.py         └─ browser_       builder      │
│  ├─ config_builder.py        launcher                   │
│  ├─ state.py                                            │
│  └─ logging_config.py                                   │
├─────────────────────────────────────────────────────────┤
│  config.py         路径/常量/核心检测/动态端口             │
│  models/profile.py ProfileItem 数据类                    │
├─────────────────────────────────────────────────────────┤
│  外部依赖：v2rayN 安装 @ d:\useful\加速器\v2rayn          │
│  ┌──────────────────┐  ┌──────────────────┐             │
│  │ sing-box (TUN)    │  │ Xray (proxy)     │             │
│  │ configPre.json    │  │ config.json      │             │
│  │ TUN + 路由 + DNS  │  │ SOCKS + 代理出站  │             │
│  └──────────────────┘  └──────────────────┘             │
└─────────────────────────────────────────────────────────┘
```
