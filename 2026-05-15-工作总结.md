# 2026-05-15 工作总结 — VPN Orchestrator 架构重构

## 核心发现

### 1. 根本原因：格式不兼容

经过深入分析，发现 `config_builder.py` 连接超时的根本原因是 **配置格式完全不兼容**：

| | v2rayN 生成的 config.json | 我们的 config_builder.py |
|---|---|---|
| **格式** | **Xray-core 格式** | sing-box native 格式 |
| **outbound 键名** | `protocol`, `settings`, `streamSettings` | `type`, `server`, `server_port`, `tls`, `transport` |
| **地址结构** | `settings.vnext[0].address`（嵌套） | `server`（扁平） |
| **路由键名** | `routing` + `rules` | `route` + `rules` |
| **DNS** | 由 v2rayN GUI 配置（不在 config.json 中） | 我们的自定义 DNS 模板 |
| **TUN** | `sockopt.dialerProxy` → `tun-protect-socks` | 无 TUN 支持 |
| **端口** | 64659 (SOCKS) + 64658 (TUN protect) | 10808 (mixed) |

**结论**：sing-box 通过内置兼容层读取 Xray 格式配置。v2rayN 生成的是 Xray 格式（经过验证的可工作配置），而我们生成的是 sing-box native 格式（完全不同，导致连接超时）。

### 2. 为什么"完全生成 config"策略必然失败

1. **格式错误** — 生成 sing-box native 格式，但 sing-box/v2rayN 生态使用 Xray 格式
2. **DNS 缺失** — 我们自定义的简单 DNS 模板与 v2rayN 的 DNS 策略不匹配
3. **路由不足** — 缺少 TUN 模式必须的路由规则（`domainStrategy`, `dialerProxy`）
4. **Inbound 不匹配** — 我们的 `mixed` inbound 替代了 v2rayN 的 `socks` + TUN 双 inbound
5. **传输细节丢失** — `sockopt`, `mux`, 特殊传输参数等被丢弃
6. **Schema drift** — sing-box 每个版本都可能删除/重命名字段

## 实施的架构重构

### Patch Strategy（补丁策略）

**核心原则**：v2rayN 负责 config compile，orchestrator 负责 runtime manipulation。

```
v2rayN GUI（一次性）
    ↓ 生成完整 config.json（Xray 格式）
    ↓ 包含：inbounds + routing + DNS + TUN + 所有 outbounds
    ↓
config.json（已验证可工作）
    ↓
Python Orchestrator（运行时）
    ↓ 只 patch proxy outbound
    ↓ 保留：inbounds, routing, dns, 其他 outbounds, TUN 설정
    ↓
sing-box.exe run -c config.json
```

### 新目录结构

```
vpn_orchestrator/
├── adapters/                     # NEW: 格式适配层
│   ├── __init__.py               # 公开 API
│   ├── xray_config.py            # Xray 格式 config 读/写/patch
│   └── outbound_builder.py       # ProfileItem → Xray/singbox outbound
├── core/
│   ├── state.py                  # ENHANCED: +FAILED, RecoveryStrategy, HealthSnapshot
│   ├── logging_config.py         # (unchanged)
│   ├── db_manager.py             # (unchanged)
│   ├── config_builder.py         # REFACTORED: delegate to adapters
│   ├── process_manager.py        # REFACTORED: delegate to RuntimeManager
│   └── runtime_manager.py        # NEW: process lifecycle + health + recovery
├── services/
│   ├── proxy_checker.py          # (unchanged)
│   ├── node_switcher.py          # (compatible, uses new config_builder)
│   └── browser_launcher.py       # (unchanged)
├── models/
│   └── profile.py                # (unchanged)
├── config.py                     # (unchanged)
├── orchestrator.py               # REFACTORED: uses RuntimeManager + new state machine
├── daemon.py                     # UPDATED: handles FAILED state
└── main.py                       # (unchanged)
```

## 新增/修改文件详情

### 1. `adapters/xray_config.py` — Config Patch Engine（新增）

职责：
- **格式检测** — `get_config_format()` 自动识别 Xray vs sing-box 格式
- **Proxy 定位** — `find_proxy_outbound()` 3-pass 搜索（tag=proxy → 非路由 outbound → fallback）
- **安全 Patch** — `patch_proxy_outbound()` 仅替换 `protocol`, `settings`, `streamSettings`，保留：
  - `mux` 配置（连接复用）
  - `sockopt.dialerProxy`（TUN 模式关键字段）
  - 所有未知字段（向前兼容）
- **备份/恢复** — `backup_config()` / `restore_config()`

### 2. `adapters/outbound_builder.py` — Outbound 生成器（新增）

支持协议（Xray 格式）：

| ConfigType | 协议 | 传输 | TLS |
|---|---|---|---|
| 1 | VMess | tcp/ws/httpupgrade/grpc | tls/none |
| 3, 4 | Shadowsocks | tcp | none |
| 5 | VLESS + Vision | httpupgrade | tls |
| 11 | VLESS | grpc | tls |

同时提供 `build_singbox_outbound()` 用于未来格式迁移。

关键设计：`_build_stream_settings()` 正确处理：
- `ws` → `wsSettings.path` + `wsSettings.headers.Host`
- `httpupgrade` → `httpupgradeSettings.path` + `host`
- `grpc` → `grpcSettings.serviceName`
- `tls` → `tlsSettings.serverName` + `fingerprint` + `allowInsecure`
- `reality` → `realitySettings` (publicKey, shortId, spiderX)

### 3. `core/state.py` — 增强状态机

新增状态：
- **FAILED** — 所有恢复策略耗尽后的终态（需人工介入）

新增组件：
- **RecoveryStrategy** 枚举 — 分级恢复策略
  ```
  0-2 次失败 → RESTART_PROCESS
  3-5 次失败 → SWITCH_NODE
  6-8 次失败 → RESET_CONFIG
  9-14次失败 → WAIT_RETRY (退避)
  15+ 次失败 → GIVE_UP → FAILED 状态
  ```
- **HealthSnapshot** — 结构化健康数据（online, latency, speed, process_running, timestamp）
- **VpnStatus** 增强：
  - `update_health()` — 统一健康数据更新
  - `get_recovery_strategy()` — 基于连续失败次数自动升级策略
  - `is_node_blacklisted()` — 节点故障黑名单
- **StateMachine** 增强：
  - `is_stable` / `is_unhealthy` — 状态分类
  - `transition_if_valid()` — 安全转换（不抛异常）

状态转换图：
```
DISCONNECTED ──→ CONNECTING ──→ CONNECTED ──→ DEGRADED
     ↑               │               │              │
     │               ↓               ↓              ↓
     └────────── FAILED ←──── RECOVERING ←──────────┘
                     │              │
                     └──→ CONNECTING (retry)
                     
STOPPED ←── (any state on user request)
```

### 4. `core/runtime_manager.py` — Runtime 编排引擎（新增）

职责：
- **进程发现** — `find_singbox_process()` 扫描系统进程（支持 pid/name/exe/cmdline）
- **进程控制** — `start()` / `stop()` / `restart()`（Windows-safe）
  - `CREATE_NO_WINDOW` 防止控制台窗口
  - 优雅终止 → 超时强制 kill
  - 启动后验证进程确实出现
  - 启动失败时解析 stderr 中的 FATAL/ERROR
- **健康检查** — `health_check()` 返回 `HealthSnapshot`
- **自动恢复** — `attempt_recovery()` 执行分级恢复策略

Windows 特别处理：
- `subprocess.CREATE_NO_WINDOW` — 防止弹出控制台
- `ENABLE_DEPRECATED_LEGACY_DNS_SERVERS=true` — sing-box 1.12+ 兼容
- 进程终止后 `time.sleep(0.5)` — 等待文件句柄释放（防 EBUSY）
- 启动后轮询 `is_running()` — 确认进程真正出现

### 5. `core/config_builder.py` — 重构为薄封装

保持公开 API 不变（`apply_profile_to_config`, `read_current_config`, `backup_config`, `restore_config`），内部委托给 `adapters/` 层。

### 6. `core/process_manager.py` — 委托给 RuntimeManager

保持公开 API 兼容，内部委托给 `RuntimeManager`。

### 7. `orchestrator.py` — 集成 RuntimeManager + 新状态机

关键变更：
- 使用 `RuntimeManager` 替代直接 `ProcessManager` 调用
- 使用 `HealthSnapshot` 替代原始 check 结果
- 添加 `_sync_initial_state()` 启动时状态同步
- `tick()` 使用 `attempt_recovery()` 替代硬编码恢复逻辑
- 节点黑名单过滤

## Schema Drift 防护设计

### 已发生的 drift

| sing-box 版本 | 变更 | 我们的防护 |
|---|---|---|
| 1.12+ | 删除 legacy DNS servers | `ENABLE_DEPRECATED_LEGACY_DNS_SERVERS=true` env var |
| 1.13 | 删除 `sniff` 字段 | 不再出现在我们的 outbound 中（使用 Xray 格式） |
| 1.13 | 删除 `encryption` 字段 | 不再出现在我们的 outbound 中（使用 Xray 格式） |
| 1.13+ | `sniff_override_destination` 删除 | 不再出现在我们的 inbound 中 |

### 防护策略

**第一层：格式隔离（adapters/）**
- Xray 格式是 v2rayN 和 sing-box 之间的稳定合约
- 即使 sing-box native 格式变化，Xray 兼容层保持稳定
- `outbound_builder.py` 只设置必要字段，不包含已知会被删除的字段

**第二层：Patch 而非 Replace**
- `patch_proxy_outbound()` 只替换 `protocol`, `settings`, `streamSettings`
- `mux`, `sockopt`, 及所有未知字段完全保留
- 新版本添加的字段自动保留（因为我们不删除未知字段）

**第三层：格式自动检测**
- `get_config_format()` 自动识别当前配置格式
- 如果 v2rayN 未来迁移到 sing-box native 格式，自动适配

**第四层：备份/恢复**
- 每次 patch 前自动备份 `config.json.bak`
- `RecoveryStrategy.RESET_CONFIG` 可自动恢复

## 测试结果

### Patch 测试（使用生产数据）

```
=== Before patch ===
Outbounds count: 2
  [0] tag=proxy protocol=shadowsocks
       sockopt.dialerProxy=tun-protect-socks
  [1] tag=tun-protect-socks protocol=socks

=== After patch ===
Outbounds count: 2
  [0] tag=proxy protocol=shadowsocks
       address=ss.030.node-for-bigairport.win port=19287
       sockopt.dialerProxy=tun-protect-socks (PRESERVED)  ✅
       mux={"enabled": false, "concurrency": -1} (PRESERVED)  ✅
  [1] tag=tun-protect-socks protocol=socks (UNCHANGED)  ✅

ALL ASSERTIONS PASSED
Non-proxy outbounds: UNCHANGED
Routing: PRESERVED
Inbounds: PRESERVED
sockopt (TUN): PRESERVED
mux: PRESERVED
```

### Import 链测试

```
adapters.xray_config OK
adapters.outbound_builder OK
core.config_builder OK
core.state OK (DISCONNECTED, CONNECTING, CONNECTED, DEGRADED, FAILED, SWITCHING, RECOVERING, STOPPED)
core.runtime_manager OK
core.process_manager OK
orchestrator OK
ALL IMPORTS SUCCESSFUL
```

## 待验证

以下项目需要在实际运行中验证（本次未执行，因为会中断当前 VPN 连接）：

1. **实际连接测试** — 用 patched config 启动 sing-box，验证节点连通性
2. **Daemon 模式** — 长期运行，验证健康检查和自动恢复
3. **VLESS 节点** — 当前数据库主要是 Shadowsocks，VLESS 节点需要实际测试
4. **TUN 模式兼容性** — 确认 `sockopt.dialerProxy` 正确保留后 TUN 仍然工作
5. **ConfigType=1 (VMess) 节点** — 数据库中有 1 个，需要测试

## 设计原则总结

1. **不要重新发明 v2rayN 的配置生成逻辑** — v2rayN 已经解决了复杂的兼容性问题
2. **Patch over Generate** — 只修改需要改的部分，保留一切其他内容
3. **格式无关** — 自动检测并适配 Xray/singbox 两种格式
4. **Schema drift 隔离** — 通过 adapters 层隔离版本变化
5. **渐进式恢复** — RESTART → SWITCH → RESET → BACKOFF → GIVE_UP
6. **Windows 优先** — 所有进程操作考虑 Windows 特殊行为

## 涉及的文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `adapters/__init__.py` | **新增** | Adapters 包入口 |
| `adapters/xray_config.py` | **新增** | Xray 格式 config patch 引擎 |
| `adapters/outbound_builder.py` | **新增** | ProfileItem → outbound 生成器 |
| `core/state.py` | **重写** | +FAILED, +RecoveryStrategy, +HealthSnapshot |
| `core/runtime_manager.py` | **新增** | Process lifecycle + health + recovery |
| `core/config_builder.py` | **重构** | 委托到 adapters，API 不变 |
| `core/process_manager.py` | **重构** | 委托到 RuntimeManager，API 不变 |
| `orchestrator.py` | **重写** | 集成 RuntimeManager + 新状态机 |
| `daemon.py` | **修改** | 处理 FAILED 状态 |
